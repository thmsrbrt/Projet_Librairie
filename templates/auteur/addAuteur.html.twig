{% extends "layout.html.twig" %}

{% block content %}
    <div class="row ">
        template/auteur/addAuteur.html.twig
        <form method="post" action="{{ path('auteur_validFormCreerAuteur') }}">
            <fieldset>
                <legend>Ajouter un Auteur</legend>

                <label>Nom
                    <input name="nom"  type="text"  size="18" value="{{ donnees.nom | default('') }}"/>
                </label>
                {% if erreurs.nom is defined %}
                    <small class="error">{{erreurs.nom}}</small>
                {% endif %}

                <label>Prénom</label>
                <input name="prenom"  type="text"  size="18" value="{{ donnees.prenom | default('') }}"/>

                <input type="submit" name="validerForm" value="valider" />
            </fieldset>
        </form>
    </div>
{% endblock %}